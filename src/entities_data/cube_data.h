#include <config.h>

struct CubeData
{
	glm::vec3 rotation = glm::vec3(0.0f, 0.0f, 0.0f);
	glm::vec3 position = glm::vec(0.0f);
	glm::vec3 color = glm::vec3(30.0f, 100.0f, 22.0f);

	float scale = 1.0f;
};